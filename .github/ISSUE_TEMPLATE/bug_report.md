---
name: Bug 报告
about: 报告控制台的问题
title: "[bug] "
labels: bug
assignees: ''
---

**先跑自检**

双击 `check.cmd`（或 `node server.cjs --check`），把完整输出贴到下面。
**注意：不要粘贴 `dsh-config.json` 的内容（含访问令牌）。**

```
（自检输出贴这里）
```

**环境**

- 控制台版本（VERSION 文件 / 包名后缀）：
- DSH 版本（`check.cmd` 第 [5] 项会显示）：
- 操作系统：
- Node 版本（`node -v`）：

**问题描述**

清楚描述遇到了什么。

**复现步骤**

1. 打开页面「…」
2. 点击「…」
3. 出现 …

**期望行为**

**实际行为**

**浏览器控制台信息**（F12 → Console / Network 里失败请求的状态码）

```
（贴这里）
```
