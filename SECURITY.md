# 安全策略

## 报告漏洞

**请不要用公开 Issue 报告安全漏洞。**

请发送邮件至 **liwei@geoscene.cn**（作者：liwei，易智瑞西安），
标题注明「[security] DSH Console」。我们会尽快确认并在修复后致谢报告人
（除非你希望匿名）。

## 威胁模型（先读这个再报）

本产品刻意只做本机工具，安全边界如下：

- 服务只绑定 **127.0.0.1**，不监听局域网/公网地址。
- DSH 访问令牌只保存在本机 `dsh-config.json`（已随 `.gitignore` 排除），
  不进代码仓库、不进产物包。
- 生产打包（`dist.cmd`）会扫描产物，防止令牌与本机路径泄漏。
- `start.cmd` / `check.cmd` 与部署自检均不联网上传任何数据。

以下情况**不算**漏洞，属于设计边界：本机其他进程可以访问 3081 端口与
配置文件（本机已被攻破的前提下，本产品不提供额外防护）。
